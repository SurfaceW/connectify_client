import React from 'react'
import classes from './EntityEditor.scss'

export const EntityEditor = () => (
  <div>
    Hello, this is entity editor page!
  </div>
)

EntityEditor.propTypes = {
}

export default EntityEditor
